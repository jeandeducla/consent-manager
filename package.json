{
  "name": "consent-manager",
  "version": "1.0.0",
  "description": "Globally manage your consent on the internet",
  "author": "jean.deducla@gmail.com",
  "license": "ISC",
  "homepage": "https://github.com/jeandeducla/consent-manager#readme",
  "scripts": {
    "prebuild": "npm run clean",
    "prebuild:debug": "npm run clean",
    "build": "parcel build src/**/* --out-dir build --no-source-maps && cp src/manifest.json build",
    "build:debug": "parcel build src/**/* --out-dir build && cp src/manifest.json build",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jeandeducla/consent-manager.git"
  },
  "bugs": {
    "url": "https://github.com/jeandeducla/consent-manager/issues"
  },
  "browserslist": [
      "since 2017-06"
  ],
  "devDependencies": {
    "@types/firefox-webext-browser": "^82.0.1",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.4.3"
  }
}
